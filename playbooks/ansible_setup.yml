---
- name: Create AWS VM for Ansible Controller
  hosts: localhost
  connection: local
  gather_facts: False
  vars_files:
    - ../vars/environment.yml
    - ../vars/credentials.yml
  roles:
    - provision_ansible_vm
    - ansible_setup