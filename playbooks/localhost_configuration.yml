---
- name: Setup localhost for AWS
  hosts: localhost
  connection: local
  gather_facts: False
  vars_files:
    - ../vars/credentials.yml
  roles:
    - create_ansible_user
    - setup_localhost
    - aws_keypair